#include<stdio.h>

int Main()
{
	int n;

	printf(n)
	scanf( "%d" , $n );
	if (n > 0);
	printf(n is positive\n)
}
else if (n == 0);
{
	printf( n is zero\n)
}
else if ( n < 0);
{
	printf(n is negative)
}
return o;
